import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";
import { CheckCircle2, ChevronRight, Database, FileCheck2, GraduationCap, Layers3, ShieldCheck, Sparkles, Target, Waypoints } from "lucide-react";
import { useLocation } from "wouter";
import { alignIqEvidence, calculateAlignment } from "@shared/aligniq";

const alignment = calculateAlignment(alignIqEvidence);
const profile = { name: "Operations Analyst pathway", target: "Business Analyst", alignment: alignment.score, confidence: alignment.confidence };
const skills = [
  { name: "Requirements analysis", score: 92, evidence: "Process maps · acceptance criteria · stakeholder briefs", band: "Strong" },
  { name: "Data analysis", score: 84, evidence: "SQL models · KPI definitions · dashboard delivery", band: "Strong" },
  { name: "Workflow automation", score: 71, evidence: "CRM rules · task triggers · audit events", band: "Developing" },
  { name: "Technical implementation", score: 66, evidence: "TypeScript · React · typed backend procedures", band: "Developing" },
];
const evidence = [
  { source: "InsightHub", type: "Project", claim: "Governed KPI and decision-intelligence dashboard", weight: "High" },
  { source: "OpsFlow", type: "Project", claim: "CRM workflow, automation rules, and audit history", weight: "High" },
  { source: "Portfolio", type: "Work sample", claim: "Analytics, automation, digital strategy, and web delivery", weight: "Medium" },
];

export default function AlignIQ() {
  const [location, setLocation] = useLocation();
  const section = location.split("/")[2] ?? "overview";
  return <div className="min-h-screen bg-[#080d18] text-slate-100">
    <div className="mb-6 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.24em] text-cyan-300">AlignIQ / {section}</p><h1 className="mt-2 font-display text-3xl font-semibold tracking-tight text-white">Make skills alignment explainable</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-400">A transparent workforce-intelligence prototype that maps evidence to an occupation without hiding assumptions behind one opaque score.</p></div><Badge className="border-cyan-300/20 bg-cyan-300/10 px-3 py-1 text-cyan-200">Synthetic portfolio data</Badge></div>
    <div className="mb-6 flex flex-wrap gap-2">{[["overview","Report builder"],["taxonomy","Taxonomy"],["evaluation","Evaluation"]].map(([slug,label]) => <Button key={slug} onClick={() => setLocation(`/aligniq/${slug}`)} variant={section === slug ? "default" : "outline"} className={section === slug ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/10 bg-white/5 text-slate-300 hover:bg-white/10"}>{label}</Button>)}</div>
    {section === "taxonomy" ? <Taxonomy /> : section === "evaluation" ? <Evaluation /> : <Overview setLocation={setLocation} />}
    <div className="mt-8 border-t border-white/10 pt-4 text-xs text-slate-600">AlignIQ build program · data foundation → taxonomy → API → accessible report → evaluation · synthetic or openly licensed data only</div>
  </div>;
}
function Overview({ setLocation }: any) { return <><div className="grid gap-4 md:grid-cols-3"><Metric label="Alignment score" value={`${profile.alignment}%`} detail="weighted evidence coverage" icon={Target} /><Metric label="Confidence band" value={profile.confidence} detail="based on evidence quality" icon={ShieldCheck} tone="cyan" /><Metric label="Evidence items" value={evidence.length} detail="traceable source records" icon={FileCheck2} /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.15fr_.85fr]"><Card className="border-white/10 bg-[#111a2a]/80"><CardHeader><div className="flex items-center justify-between"><div><CardTitle className="text-base text-white">Explainable alignment report</CardTitle><p className="mt-1 text-xs text-slate-500">{profile.name} → {profile.target}</p></div><Sparkles className="h-5 w-5 text-cyan-300" /></div></CardHeader><CardContent className="space-y-5"><div className="rounded-2xl border border-cyan-300/15 bg-cyan-300/[0.06] p-5"><div className="flex items-end justify-between"><div><p className="text-xs uppercase tracking-wider text-cyan-200">Overall alignment</p><p className="mt-2 text-4xl font-semibold text-white">{profile.alignment}%</p></div><span className="text-right text-xs text-slate-400">No score is final<br />without evidence review</span></div><Progress value={profile.alignment} className="mt-4 h-2 bg-white/10" /></div>{skills.map((skill) => <div key={skill.name}><div className="flex items-center justify-between text-sm"><span className="font-medium text-slate-200">{skill.name}</span><span className="text-cyan-200">{skill.score}%</span></div><Progress value={skill.score} className="mt-2 h-2 bg-white/10" /><p className="mt-1 text-xs text-slate-500">{skill.band} · {skill.evidence}</p></div>)}</CardContent></Card><Card className="border-white/10 bg-[#111a2a]/80"><CardHeader><CardTitle className="text-base text-white">Evidence traceability</CardTitle><p className="mt-1 text-xs text-slate-500">Every claim can be inspected</p></CardHeader><CardContent className="space-y-3">{evidence.map((item) => <div key={item.source} className="rounded-xl border border-white/8 bg-white/[0.025] p-4"><div className="flex items-center justify-between"><span className="text-sm font-medium text-white">{item.source}</span><Badge className="border-white/10 bg-white/5 text-slate-400">{item.weight}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{item.claim}</p><p className="mt-2 text-xs text-slate-600">{item.type} · source record linked</p></div>)}<Button onClick={() => setLocation("/aligniq/evaluation")} variant="outline" className="mt-2 w-full border-cyan-300/20 bg-cyan-300/5 text-cyan-200">Inspect evaluation and limitations <ChevronRight className="ml-2 h-4 w-4" /></Button></CardContent></Card></div><div className="mt-6 grid gap-4 md:grid-cols-3"><Feature icon={Layers3} title="Versioned model" body="Weights, evidence types, and confidence rules are explicit." /><Feature icon={Waypoints} title="Report API" body="Profile, occupation, evidence, and report boundaries are documented." /><Feature icon={GraduationCap} title="Learning signal" body="Gaps become a practical development plan, not a rejection label." /></div></>; }
function Taxonomy() { return <div className="grid gap-6 xl:grid-cols-[.9fr_1.1fr]"><Card className="border-white/10 bg-[#111a2a]/80"><CardHeader><CardTitle className="text-base text-white">Occupation taxonomy</CardTitle><p className="mt-1 text-xs text-slate-500">The model vocabulary behind the report</p></CardHeader><CardContent className="space-y-3">{[["Business Analyst","requirements · process · stakeholders"],["Data Analyst","SQL · metrics · visualization"],["Automation Specialist","triggers · systems · auditability"],["Junior Software Engineer","frontend · APIs · testing"]].map(([name,desc]) => <div key={name} className="flex items-center gap-3 rounded-xl border border-white/8 p-4"><Database className="h-4 w-4 text-cyan-300" /><div><p className="text-sm font-medium text-white">{name}</p><p className="mt-1 text-xs text-slate-500">{desc}</p></div></div>)}</CardContent></Card><Card className="border-white/10 bg-[#111a2a]/80"><CardHeader><CardTitle className="text-base text-white">Skill → evidence relationships</CardTitle><p className="mt-1 text-xs text-slate-500">A compact data model a recruiter can inspect</p></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-xs uppercase tracking-wider text-slate-600"><tr><th className="pb-3">Skill</th><th className="pb-3">Evidence type</th><th className="pb-3">Confidence</th></tr></thead><tbody>{skills.map((skill) => <tr key={skill.name} className="border-t border-white/8"><td className="py-4 text-slate-200">{skill.name}</td><td className="py-4 text-slate-400">Project artifact</td><td className="py-4 text-cyan-200">{skill.band}</td></tr>)}</tbody></table></div></CardContent></Card></div>; }
function Evaluation() { const checks = [{ name: "Evidence traceability", value: "100%", detail: "Each report claim links to a source record" }, { name: "Empty evidence handling", value: "Pass", detail: "No-evidence profiles return an explainable gap state" }, { name: "Confidence calibration", value: "Pass", detail: "Low-quality evidence cannot produce high confidence" }, { name: "Bias limitation", value: "Documented", detail: "Occupation taxonomies can encode institutional bias" }]; return <div className="grid gap-6 xl:grid-cols-[1fr_1fr]"><Card className="border-white/10 bg-[#111a2a]/80"><CardHeader><CardTitle className="text-base text-white">Reproducibility checks</CardTitle><p className="mt-1 text-xs text-slate-500">Fixtures and edge cases for a responsible score</p></CardHeader><CardContent className="space-y-3">{checks.map((check) => <div key={check.name} className="flex items-start gap-3 rounded-xl border border-white/8 p-4"><CheckCircle2 className="mt-0.5 h-4 w-4 text-emerald-300" /><div><div className="flex flex-wrap gap-2"><span className="text-sm font-medium text-white">{check.name}</span><Badge className="border-emerald-300/20 bg-emerald-300/10 text-emerald-200">{check.value}</Badge></div><p className="mt-1 text-xs leading-5 text-slate-500">{check.detail}</p></div></div>)}</CardContent></Card><Card className="border-white/10 bg-[#111a2a]/80"><CardHeader><CardTitle className="text-base text-white">Known limitations</CardTitle></CardHeader><CardContent className="space-y-4 text-sm leading-6 text-slate-400"><p>Alignment is a decision-support signal, not a hiring decision. It is sensitive to taxonomy quality, evidence freshness, and the completeness of the profile.</p><p>The demonstration uses synthetic portfolio records. A real implementation would require governance, consent, versioned occupation sources, and stakeholder review before operational use.</p><p>Evaluation should compare outcomes across groups and inspect false positives and false negatives rather than optimizing one aggregate score.</p></CardContent></Card></div>; }
function Metric({ label, value, detail, icon: Icon, tone = "cyan" }: any) { return <Card className="border-white/10 bg-[#111a2a]/80"><CardContent className="p-5"><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{label}</p><Icon className={`h-4 w-4 ${tone === "cyan" ? "text-cyan-300" : "text-emerald-300"}`} /></div><p className="mt-4 text-3xl font-semibold text-white">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></CardContent></Card>; }
function Feature({ icon: Icon, title, body }: any) { return <div className="rounded-2xl border border-white/10 bg-[#111a2a]/70 p-5"><Icon className="h-5 w-5 text-cyan-300" /><p className="mt-4 text-sm font-semibold text-white">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{body}</p></div>; }
