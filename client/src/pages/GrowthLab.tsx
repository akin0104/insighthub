import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";
import { BarChart3, CheckCircle2, FlaskConical, Globe2, Mail, MousePointerClick, Search, ShieldCheck, Sparkles, TrendingUp } from "lucide-react";
import { useLocation } from "wouter";
import { campaignRows, evaluateExperiment, funnelRate, growthEvents } from "@shared/growthlab";

export default function GrowthLab() { const [location, setLocation] = useLocation(); const section = location.split("/")[2] ?? "overview"; const conversion = funnelRate(growthEvents[0].count, growthEvents[2].count); const lift = evaluateExperiment(7.6, 8.6).lift; return <div className="min-h-screen bg-[#0b0f18] text-slate-100"><div className="mb-6 flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.24em] text-orange-300">GrowthLab / {section}</p><h1 className="mt-2 font-display text-3xl font-semibold tracking-tight text-white">Turn digital activity into decisions</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-400">A measurable digital-strategy workspace connecting SEO, content, social, email, analytics, and conversion optimization.</p></div><Badge className="border-orange-300/20 bg-orange-300/10 px-3 py-1 text-orange-200">Synthetic event simulator</Badge></div><div className="mb-6 flex flex-wrap gap-2">{[["overview","Funnel overview"],["events","Event taxonomy"],["experiments","Experiments"]].map(([slug,label]) => <Button key={slug} onClick={() => setLocation(`/growthlab/${slug}`)} variant={section === slug ? "default" : "outline"} className={section === slug ? "bg-orange-300 text-slate-950 hover:bg-orange-200" : "border-white/10 bg-white/5 text-slate-300 hover:bg-white/10"}>{label}</Button>)}</div>{section === "events" ? <Events /> : section === "experiments" ? <Experiments lift={lift} /> : <Overview conversion={conversion} setLocation={setLocation} />}</div>; }
function Overview({ conversion, setLocation }: any) { return <><div className="grid gap-4 md:grid-cols-4"><Metric label="Sessions" value="1,240" detail="privacy-aware events" icon={Globe2} /><Metric label="Leads" value="74" detail="purpose-limited submit" icon={MousePointerClick} tone="orange" /><Metric label="Site conversion" value={`${conversion}%`} detail="session → lead" icon={TrendingUp} tone="green" /><Metric label="Best campaign" value="8.6%" detail="email case study" icon={Mail} tone="orange" /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1fr_.9fr]"><Card className="border-white/10 bg-[#151a28]/80"><CardHeader><CardTitle className="text-base text-white">Conversion journey</CardTitle><p className="mt-1 text-xs text-slate-500">Acquisition → engagement → conversion → activation</p></CardHeader><CardContent className="space-y-5">{growthEvents.map((event, index) => <div key={event.name}><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-orange-300/10 text-xs font-semibold text-orange-200">0{index + 1}</span><div><p className="text-sm font-medium text-white">{event.name}</p><p className="text-xs text-slate-500">{event.stage} · {event.privacy}</p></div></div><span className="text-sm text-orange-200">{event.count.toLocaleString()}</span></div><Progress value={event.count / 1240 * 100} className="mt-3 h-2 bg-white/10" /></div>)}</CardContent></Card><Card className="border-white/10 bg-[#151a28]/80"><CardHeader><CardTitle className="text-base text-white">Campaign intelligence</CardTitle><p className="mt-1 text-xs text-slate-500">Channel performance with a clear decision path</p></CardHeader><CardContent className="space-y-3">{campaignRows.map((row) => <div key={row.campaign} className="rounded-xl border border-white/8 bg-white/[0.025] p-4"><div className="flex items-center justify-between"><span className="text-sm font-medium text-white">{row.campaign}</span><span className="text-sm text-orange-200">{row.conversion}%</span></div><div className="mt-2 flex justify-between text-xs text-slate-500"><span>{row.leads} leads / {row.sessions} sessions</span><span>${row.spend} spend</span></div></div>)}<Button onClick={() => setLocation("/growthlab/experiments")} variant="outline" className="mt-2 w-full border-orange-300/20 bg-orange-300/5 text-orange-200">Plan the next experiment <FlaskConical className="ml-2 h-4 w-4" /></Button></CardContent></Card></div><div className="mt-6 grid gap-4 md:grid-cols-3"><Feature icon={Search} title="SEO and content" body="Connect intent, content performance, and measurable downstream action." /><Feature icon={BarChart3} title="Funnel analytics" body="Use SQL-ready events to expose cohort and campaign drop-off." /><Feature icon={ShieldCheck} title="Privacy by design" body="Keep event purpose and identifier boundaries visible to reviewers." /></div></>; }
function Events() { return <div className="grid gap-6 xl:grid-cols-[1fr_.8fr]"><Card className="border-white/10 bg-[#151a28]/80"><CardHeader><CardTitle className="text-base text-white">Event taxonomy</CardTitle><p className="mt-1 text-xs text-slate-500">A tracking plan that a marketing and engineering team can share</p></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-xs uppercase tracking-wider text-slate-600"><tr><th className="pb-3">Event</th><th className="pb-3">Stage</th><th className="pb-3">Privacy boundary</th></tr></thead><tbody>{growthEvents.map((event) => <tr key={event.name} className="border-t border-white/8"><td className="py-4 font-medium text-white">{event.name}</td><td className="py-4 text-slate-400">{event.stage}</td><td className="py-4 text-orange-200">{event.privacy}</td></tr>)}</tbody></table></div></CardContent></Card><Card className="border-white/10 bg-[#151a28]/80"><CardHeader><CardTitle className="text-base text-white">Implementation notes</CardTitle></CardHeader><CardContent className="space-y-4 text-sm leading-6 text-slate-400"><p>Each event has a defined business purpose, stage, and privacy boundary before it becomes a dashboard metric.</p><p>Real analytics would require consent signals, retention rules, identity minimization, and a documented data dictionary.</p><div className="rounded-xl border border-orange-300/15 bg-orange-300/[0.05] p-4 text-orange-100"><Sparkles className="mb-2 h-4 w-4" />The tracking plan is the contract between digital strategy, analytics, and engineering.</div></CardContent></Card></div>; }
function Experiments({ lift }: any) { return <div className="grid gap-6 xl:grid-cols-[1fr_.85fr]"><Card className="border-white/10 bg-[#151a28]/80"><CardHeader><CardTitle className="text-base text-white">Experiment brief</CardTitle><p className="mt-1 text-xs text-slate-500">A decision-ready test plan</p></CardHeader><CardContent className="space-y-4">{[["Hypothesis","A clearer case-study CTA will increase lead submissions."],["Primary metric","Lead-submit rate from engaged sessions."],["Guardrails","Bounce rate, qualified-call rate, and unsubscribe rate."],["Interpretation","Ship only when the lift is meaningful and guardrails remain healthy."]].map(([label,value]) => <div key={label} className="rounded-xl border border-white/8 p-4"><p className="text-xs font-semibold uppercase tracking-wider text-orange-300">{label}</p><p className="mt-1 text-sm leading-6 text-slate-300">{value}</p></div>)}</CardContent></Card><Card className="border-white/10 bg-[#151a28]/80"><CardHeader><CardTitle className="text-base text-white">Variant readout</CardTitle></CardHeader><CardContent><p className="text-sm text-slate-400">Control: 7.6% · Variant: 8.6%</p><p className="mt-4 text-4xl font-semibold text-white">+{lift}%</p><p className="mt-1 text-xs text-slate-500">illustrative lift from synthetic data</p><div className="mt-6 flex items-center gap-2 text-sm text-emerald-200"><CheckCircle2 className="h-4 w-4" />Guardrail review required before shipping</div><div className="mt-6 rounded-xl border border-amber-300/15 bg-amber-300/[0.05] p-4 text-xs leading-5 text-slate-400">This is a portfolio experiment design, not a claim about real campaign performance.</div></CardContent></Card></div>; }
function Metric({ icon: Icon, label, value, detail, tone = "orange" }: any) { return <Card className="border-white/10 bg-[#151a28]/80"><CardContent className="p-5"><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{label}</p><Icon className={`h-4 w-4 ${tone === "green" ? "text-emerald-300" : "text-orange-300"}`} /></div><p className="mt-4 text-3xl font-semibold text-white">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></CardContent></Card>; }
function Feature({ icon: Icon, title, body }: any) { return <div className="rounded-2xl border border-white/10 bg-[#151a28]/70 p-5"><Icon className="h-5 w-5 text-orange-300" /><p className="mt-4 text-sm font-semibold text-white">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{body}</p></div>; }
