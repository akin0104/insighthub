import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowRight, BarChart3, CheckCircle2, Layers3, ShieldCheck } from "lucide-react";
import { useLocation } from "wouter";

export default function GrowthLanding() { const [, setLocation] = useLocation(); return <main className="min-h-screen bg-[#0b0f18] text-slate-100"><section className="mx-auto max-w-6xl px-4 py-16 sm:px-8 lg:py-24"><div className="max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.24em] text-orange-300">GrowthLab / accessible campaign journey</p><h1 className="mt-5 font-display text-4xl font-semibold tracking-tight text-white sm:text-6xl">Make every click answer a business question.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-400">A measurable operating model for teams that want to connect content, SEO, social, email, and conversion decisions without losing sight of privacy or accessibility.</p><div className="mt-8 flex flex-wrap gap-3"><Button onClick={() => setLocation("/growthlab/overview")} className="bg-orange-300 text-slate-950 hover:bg-orange-200">Explore the funnel <ArrowRight className="ml-2 h-4 w-4" /></Button><Button onClick={() => setLocation("/growthlab/events")} variant="outline" className="border-white/10 bg-white/5 text-slate-300">View tracking plan</Button></div></div><div className="mt-14"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-orange-300">Conversion journey</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["01","Discover","Search, content, and social create qualified attention."],["02","Engage","A useful CTA turns attention into an intentional interaction."],["03","Convert","A lead submission captures a purpose-limited business signal."],["04","Activate","A qualified call creates the handoff to operations." ]].map(([step,title,body]) => <div key={step} className="rounded-2xl border border-orange-300/15 bg-orange-300/[0.04] p-4"><p className="text-xs font-semibold text-orange-300">{step}</p><h2 className="mt-3 text-sm font-semibold text-white">{title}</h2><p className="mt-2 text-xs leading-5 text-slate-500">{body}</p></div>)}</div></div><div className="mt-16 grid gap-4 md:grid-cols-3">{[[BarChart3,"Measure the journey","See acquisition, engagement, lead submit, and activation as one connected story."],[Layers3,"Improve the workflow","Turn campaign signals into experiments with clear hypotheses and guardrails."],[ShieldCheck,"Respect the user","Define purpose, consent, and identifier boundaries before tracking." ]].map(([Icon,title,body]: any) => <Card key={title} className="border-white/10 bg-[#151a28]/80"><CardContent className="p-6"><Icon className="h-6 w-6 text-orange-300" /><h2 className="mt-5 text-lg font-semibold text-white">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{body}</p></CardContent></Card>)}</div></section><section className="border-y border-white/10 bg-white/[0.02]"><div className="mx-auto grid max-w-6xl gap-8 px-4 py-12 sm:px-8 md:grid-cols-[1fr_auto] md:items-center"><div><p className="text-sm font-semibold text-white">A conversion journey is more than a button.</p><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">The event contract makes the handoff from marketing to analytics to engineering explicit, testable, and reviewable.</p></div><div className="flex items-center gap-2 text-sm text-emerald-200"><CheckCircle2 className="h-4 w-4" />Accessible CTA path</div></div></section></main>; }
