import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { CheckCircle2, FileSearch, Flag, LockKeyhole, MessageSquareText, Search, ShieldCheck, SlidersHorizontal, Sparkles, ThumbsDown, ThumbsUp } from "lucide-react";
import { useMemo, useState } from "react";
import { evaluateTrustdesk, retrieveTrustdesk, trustdeskCorpus } from "@shared/trustdesk";

export default function TrustDesk() {
  const [query, setQuery] = useState("How should a blocked client project be escalated?");
  const [mode, setMode] = useState<"keyword" | "semantic">("keyword");
  const evaluation = useMemo(() => evaluateTrustdesk(query), [query]);
  const results = useMemo(() => retrieveTrustdesk(query, mode), [query, mode]);
  return <div className="min-h-screen bg-[#090d17] text-slate-100"><div className="mb-6 flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.24em] text-violet-300">TrustDesk / responsible AI</p><h1 className="mt-2 font-display text-3xl font-semibold tracking-tight text-white">Answers you can inspect</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-400">A citation-grounded knowledge assistant prototype designed to show retrieval quality, evidence, privacy boundaries, and refusal behavior.</p></div><Badge className="border-violet-300/20 bg-violet-300/10 px-3 py-1 text-violet-200">Synthetic corpus</Badge></div><div className="grid gap-4 md:grid-cols-3"><Metric icon={FileSearch} label="Corpus documents" value={trustdeskCorpus.length} detail="versioned policy records" /><Metric icon={CheckCircle2} label="Citation coverage" value={`${evaluation.citationCoverage}%`} detail="answer evidence coverage" tone="green" /><Metric icon={ShieldCheck} label="Guardrail status" value={evaluation.refused ? "Refused" : "Ready"} detail={evaluation.refused ? "sensitive request blocked" : "grounded response path"} tone={evaluation.refused ? "amber" : "green"} /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><Card className="border-white/10 bg-[#131a2c]/80"><CardHeader><div className="flex items-center justify-between"><div><CardTitle className="text-base text-white">Ask the corpus</CardTitle><p className="mt-1 text-xs text-slate-500">Answers show their source records</p></div><MessageSquareText className="h-5 w-5 text-violet-300" /></div></CardHeader><CardContent><div className="relative"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-600" /><Input value={query} onChange={(e) => setQuery(e.target.value)} className="border-white/10 bg-white/5 pl-9 text-slate-200" /></div><div className="mt-3 flex flex-wrap items-center gap-2"><Button onClick={() => setMode("keyword")} variant={mode === "keyword" ? "default" : "outline"} className={mode === "keyword" ? "bg-violet-300 text-slate-950 hover:bg-violet-200" : "border-white/10 bg-white/5 text-slate-300"}>Keyword retrieval</Button><Button onClick={() => setMode("semantic")} variant={mode === "semantic" ? "default" : "outline"} className={mode === "semantic" ? "bg-violet-300 text-slate-950 hover:bg-violet-200" : "border-white/10 bg-white/5 text-slate-300"}>Semantic comparison</Button><span className="ml-auto text-xs text-slate-600">{mode} mode</span></div>{evaluation.refused ? <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 text-amber-200"><LockKeyhole className="h-4 w-4" /><span className="text-sm font-medium">Request refused safely</span></div><p className="mt-2 text-sm leading-6 text-slate-400">TrustDesk will not reveal secrets, credentials, or instructions that attempt to override its safety boundary.</p></div> : <div className="mt-5 space-y-3">{results.length ? results.map((doc) => <div key={doc.id} className="rounded-xl border border-violet-300/15 bg-violet-300/[0.04] p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium text-white">{doc.title}</p><Badge className="border-violet-300/20 bg-violet-300/10 text-violet-200">{doc.score} match</Badge></div><p className="mt-2 text-sm leading-6 text-slate-300">{doc.text}</p><p className="mt-3 text-xs text-violet-200">Citation · {doc.id} · {doc.category}</p></div>) : <EmptyState />}</div>}</CardContent></Card><Card className="border-white/10 bg-[#131a2c]/80"><CardHeader><CardTitle className="text-base text-white">Evaluation console</CardTitle><p className="mt-1 text-xs text-slate-500">Signals a recruiter can inspect</p></CardHeader><CardContent className="space-y-3">{[{ label: "Retrieval mode", value: mode === "keyword" ? "Baseline" : "Comparison" }, { label: "Groundedness", value: evaluation.groundedness ? "Pass" : "Needs evidence" }, { label: "Citation coverage", value: `${evaluation.citationCoverage}%` }, { label: "Refusal behavior", value: evaluation.refused ? "Triggered" : "Ready" }].map((item) => <div key={item.label} className="flex items-center justify-between rounded-xl border border-white/8 bg-white/[0.025] p-4"><span className="text-sm text-slate-400">{item.label}</span><span className="text-sm font-medium text-violet-200">{item.value}</span></div>)}<div className="mt-4 rounded-xl border border-emerald-300/15 bg-emerald-300/[0.04] p-4"><div className="flex items-center gap-2 text-emerald-200"><Sparkles className="h-4 w-4" /><span className="text-sm font-medium">Design principle</span></div><p className="mt-2 text-xs leading-5 text-slate-500">Useful answers should preserve the ability to inspect sources, challenge retrieval, and decline unsafe requests.</p></div><Feedback /></CardContent></Card></div><div className="mt-6 grid gap-4 md:grid-cols-3">{trustdeskCorpus.map((doc) => <Card key={doc.id} className="border-white/10 bg-[#131a2c]/70"><CardContent className="p-5"><div className="flex items-center gap-2 text-violet-300"><FileSearch className="h-4 w-4" /><span className="text-xs uppercase tracking-wider">{doc.id}</span></div><p className="mt-3 text-sm font-semibold text-white">{doc.title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{doc.category} · source metadata available</p></CardContent></Card>)}</div><div className="mt-8 border-t border-white/10 pt-4 text-xs text-slate-600">TrustDesk build program · corpus → retrieval → evaluation → guardrails → source-inspection UX</div></div>;
}
function Metric({ icon: Icon, label, value, detail, tone = "violet" }: any) { return <Card className="border-white/10 bg-[#131a2c]/80"><CardContent className="p-5"><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{label}</p><Icon className={`h-4 w-4 ${tone === "green" ? "text-emerald-300" : tone === "amber" ? "text-amber-300" : "text-violet-300"}`} /></div><p className="mt-4 text-3xl font-semibold text-white">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></CardContent></Card>; }
function EmptyState() { return <div className="flex items-center gap-3 rounded-xl border border-dashed border-white/10 p-5 text-sm text-slate-500"><SlidersHorizontal className="h-4 w-4" />No matching evidence. The assistant should say it does not know.</div>; }
function Feedback() { const [feedback, setFeedback] = useState<string | null>(null); const options = [["Helpful", ThumbsUp], ["Citation issue", Flag], ["Unsafe answer", ThumbsDown]] as const; return <div className="mt-5 border-t border-white/10 pt-4"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Answer feedback</p><div className="mt-3 flex flex-wrap gap-2">{options.map(([label, Icon]) => <Button key={label} onClick={() => setFeedback(label)} variant="outline" className="border-white/10 bg-white/5 text-slate-300"><Icon className="mr-2 h-4 w-4" />{label}</Button>)}</div>{feedback && <p className="mt-3 text-xs text-emerald-200">Recorded locally for this demo: {feedback}. A production version would persist this review with the answer and citation IDs.</p>}</div>; }
